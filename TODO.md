# TODO: Replace SVG with Image in Hero Section

- [x] Remove the CameraIcon function definition from pages/Home.tsx
- [x] Replace the <CameraIcon /> component with an <img> tag using the provided image URL in the hero section
